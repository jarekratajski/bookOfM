module MLaws where

--instance Monoid (Maybe a) where
--    mempty = Nothing
--    mappend a Nothing  =  a
--    mappend  Nothing b = b
--    mappend (Just a) (Just b) = Just a

-- mappend1::Maybe a -> Maybe a-> Maybe a
